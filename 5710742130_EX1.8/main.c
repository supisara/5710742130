#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,x;
    printf("High: ");
    scanf ("%d",&a);
    printf ("Wide: ");
    scanf ("%d",&b);
    x = (a*b)/2;
    printf ("Sum: %d",x);
    return 0;
}
